/**
 * v1 autotrader sizing: uses the same policy caps as all players. Values are
 * chosen to be the smallest "normal" trade that still clears the swap fee
 * floor with typical `wallet_policies` defaults.
 */
export const V1_BUY_STATED_USDC = 1;
export const V1_SELL_PERCENT = 25;
