/**
 * Canonical identity for the Commodus bot account. The handle has changed
 * before (`@commodus` → `@commo`) so every surface that renders it must import
 * from here rather than inlining a literal.
 */
export const COMMAND_BOT_HANDLE = "@commo";
export const COMMAND_BOT_FID = 3318962;
